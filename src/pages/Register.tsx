import { RegisterForm } from "../components"

const Register = () => {
  return (
    <div style={{
      display: "flex",
      alignItems: "center",
      minHeight: "100vh",
    }}>
      <RegisterForm />
    </div>
  )
}

export default Register